<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.2.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.2.xsd">
    <context:component-scan base-package="cn.music.dao"/>
    <!--扫描包中注解标注的类,使用注解一定要加上这行代码-->
    <!--&lt;!&ndash;引入Properties文件&ndash;&gt;
    <bean id="dataSource1" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="location">
            <value>classpath:database.properties</value>
        </property>
    </bean>

    &lt;!&ndash;建立applicationConfig-mybatis.xml配置数据源信息&ndash;&gt;
    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${user}"/>
        <property name="password" value="${password}"/>
    </bean>
    &lt;!&ndash; 配置SqlSessionFacatoryBean(注入映射器)&ndash;&gt;
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        &lt;!&ndash;引用数据源组件&ndash;&gt;
        <property name="dataSource" ref="dataSource"/>
        &lt;!&ndash;  为实体类配置别名&ndash;&gt;
        <property name="typeAliasesPackage" value="cn.music.pojo"/>
        &lt;!&ndash;导入mybatis-config.xml配置文件，为resultMap设置自动映射级别&ndash;&gt;
        <property name="configLocation" value="classpath:mybatis-config.xml"/>
    </bean>

    &lt;!&ndash;定义事务管理器&ndash;&gt;
    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    &lt;!&ndash;定义属性，声明事务规则&ndash;&gt;
    <tx:advice id="txAdvice" transaction-manager="txManager">
        <tx:attributes>
            <tx:method name="add*" propagation="SUPPORTS"/>
            <tx:method name="*" propagation="REQUIRED"/>
        </tx:attributes>
    </tx:advice>
    &lt;!&ndash; 定义切面&ndash;&gt;
    <aop:config>
        &lt;!&ndash; 定义切入点&ndash;&gt;
        <aop:pointcut id="serviceMethod" expression="execution(* cn.music.service..*.*(..))"/>
        &lt;!&ndash;将事务增强与切入点结合(前置)&ndash;&gt;
        <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceMethod"/>
    </aop:config>

    &lt;!&ndash;使用mapper扫描包,可以动态的注册MapperFactoryBean,如此即可批量产生映射器实现类
        每个实现类的名称默认为映射器的类名首字母小写&ndash;&gt;
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="cn.bdqn.dao"/>
    </bean>-->
</beans>
